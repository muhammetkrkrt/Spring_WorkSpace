server:
  port: 9090

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/moviesAppDb
    username: postgres
    password: 12345678
  jpa:
    hibernate:
      ddl-auto: update
